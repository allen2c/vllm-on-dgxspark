# Agents — Python style

- Pythonic, elegant, declared: idioms, simple flow; explicit names, APIs, and types that clarify intent.
- Module order: docstring → imports → constants → public functions → public classes → private (if any).
- Match the file’s existing layout on small edits; do not reshuffle unrelated code.
